<ul id=links>
  <%@links.each do |link| %>
  <li>
    Link: <a href='<%= link.url %>' target='_blank'><%= link.title %></a> <%= link.tags.inject("") { |sum,tag| sum << tag.name+" " } %>
  </li>
  <%end%>
</ul>
