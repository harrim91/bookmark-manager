<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <ul id='links'>
      <% @links.each do |link| %>
        <li>
          Bookmark: <a href='<%= link.url %>' target='_blank'><%= link.title %></a>
        </li>
      <% end %>
    </ul>
  </body>
</html>
