<h1> LINKS </h1>
<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%=link.title%>
      URL <%=link.url%>
    </li>
  <% end %>
</ul>

<form action='/links/new'>
  <input type='submit' value='Add new link' name='Add new link'/>
</form>
